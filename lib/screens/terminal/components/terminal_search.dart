import 'package:flutter/material.dart';

class TerminalSearchButton extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return IconButton(icon: Icon(Icons.search), onPressed: null);
  }
}
